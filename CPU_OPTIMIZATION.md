# CPU環境最適化ガイド

## 実装済みの最適化

### 1. LLM API呼び出しの最適化
- **ニュース分類の制限**: LLM分類は最大5件まで（最新の重要そうなもの優先）
- **タイムアウト短縮**: 10秒 → 5秒（CPU環境でのレスポンス時間を考慮）
- **フォールバック**: LLM APIエラー時は自動的にキーワードベース分類に切り替え

### 2. バックテストの最適化
- **API呼び出し削減**: 現在価格は履歴データから取得（追加API呼び出し不要）
- **進捗表示**: 10日ごとに進捗を表示（長時間実行時の可視化）

### 3. レート制限対策
- **待機時間**: リクエスト間に適切な待機時間を設定
- **エラーハンドリング**: レート制限エラー時の自動リトライ

## CPU環境での推奨設定

### config.json
```json
{
  "llm_api_key": "your-key",
  "llm_model": "gpt-4o-mini",  // 軽量モデル推奨
  "llm_temperature": 0.3,
  "max_assets": 50  // CPU環境では50-100資産程度が推奨
}
```

### パフォーマンス目安
- **1資産あたりの処理時間**: 約2-5秒（LLM使用時）
- **50資産の処理時間**: 約2-4分
- **100資産の処理時間**: 約4-8分

### LLM APIキーがない場合
- キーワードベース分類を使用（処理速度: 約0.5秒/資産）
- 50資産の処理時間: 約30秒

## トラブルシューティング

### メモリ使用量
- 通常の実行では100-200MB程度
- バックテスト実行時は500MB-1GB程度

### CPU使用率
- 通常の実行では10-20%（主にI/O待機）
- LLM API呼び出し時は一時的に30-50%に上昇

### タイムアウトエラー
- LLM APIのタイムアウトは5秒に設定済み
- ネットワークが遅い場合は`knowledge/event_classifier.py`の`timeout`値を調整

## さらなる最適化（オプション）

### 並列処理（将来拡張）
- 複数資産の分析を並列化（`multiprocessing`使用）
- LLM API呼び出しのバッチ処理

### キャッシュ強化
- LLM分類結果のキャッシュ（同じニュースを再分類しない）
- 財務データのキャッシュ期間延長
